package com.foliarace.core.runtime;

public enum AdapterCapability {
    RUNTIME_IDENTITY,
    GLOBAL_SCHEDULER,
    REGION_SCHEDULER,
    ASYNC_SCHEDULER,
    ENTITY_SCHEDULER,
    REGION_OWNERSHIP_CHECK,
    ENTITY_OWNERSHIP_CHECK
}
